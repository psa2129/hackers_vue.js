apiVersion: v1
kind: Service
metadata:
  name: sk041-vue-service
  namespace: class-2
spec:
  selector:
    app: sk041-vue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: test-task-sk041
  namespace: class-2
spec:
  params:
    - name: app-name
      type: string
  steps:
    - name: test
      image: alpine:3.20
      script: |
        #!/bin/sh
        echo "Testing $(params.app-name)"
